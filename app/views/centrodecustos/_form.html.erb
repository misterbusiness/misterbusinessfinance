<%= form_for(@centrodecusto) do |f| %>
  <% if @centrodecusto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@centrodecusto.errors.count, "error") %> prohibited this centrodecusto from being saved:</h2>

      <ul>
      <% @centrodecusto.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %><br />
    <%= f.text_field :descricao %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
