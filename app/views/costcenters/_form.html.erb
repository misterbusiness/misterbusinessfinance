<%= form_for(@costcenter) do |f| %>
  <% if @costcenter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@costcenter.errors.count, "error") %> prohibited this costcenter from being saved:</h2>

      <ul>
      <% @costcenter.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %><br />
    <%= f.text_field :descricao %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
