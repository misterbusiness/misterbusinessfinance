//$(document).ready(function () {
function prepareDatavencimento(){
    $('#data_vencimento_picker').datepicker({
        format: 'dd-mm-yyyy',
        autoclose: true,
        todaybtn: true
    });
//});
    $('#data_vencimento_picker').change(function(){
        try
        {
            agendamentoEndDate();
        }
        catch (err)
        {
            alert(err.message);
        }
    });

}