$(document).ready(function(){

    buildSelect2("lancamento_category", "Entre com a categoria",
            '<%= Rails.application.routes.url_helpers.list_categories_path %>',
            'category_hidden');

    $("#lancamento_category").change(function(e)
    {
        $("#category_hidden").val(e.added.text);
    });
});